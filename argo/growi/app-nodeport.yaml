apiVersion: v1
kind: Service
metadata:
  name: app-nodeport
  namespace: growi
  labels:
    io.kompose.service: app
spec:
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
      nodePort: 30080  # 30000-32767の範囲で指定
  selector:
    io.kompose.service: app
